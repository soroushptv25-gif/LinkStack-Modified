<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <menuitem id="menu_dbc_root"
              name="Business Cards"
              sequence="90"/>

    <menuitem id="menu_dbc_cards"
              name="Cards"
              parent="menu_dbc_root"
              action="action_dbc"
              sequence="10"/>

    <!-- Publish targets hold upload credentials, so admins only. -->
    <menuitem id="menu_dbc_targets"
              name="Publish Targets"
              parent="menu_dbc_root"
              action="action_dbc_target"
              groups="base.group_system"
              sequence="30"/>

    <!-- Configuration > Settings (admins only). -->
    <menuitem id="menu_dbc_config"
              name="Configuration"
              parent="menu_dbc_root"
              groups="base.group_system"
              sequence="90"/>
    <menuitem id="menu_dbc_settings"
              name="Settings"
              parent="menu_dbc_config"
              action="action_dbc_settings"
              groups="base.group_system"
              sequence="10"/>

</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.form.inherit.digital_business_card</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Business Cards" string="Business Cards"
                     name="digital_business_card"
                     logo="/digital_business_card/static/description/icon.png">
                    <block title="Public Cards" name="dbc_public_block">
                        <setting string="Card Email Source"
                                 help="Which employee email a card shows by default (a per-card Email mask still overrides it).">
                            <field name="dbc_employee_email_field"/>
                        </setting>
                        <setting string="Default Public Page Design"
                                 help="Design applied to new cards' public pages. Each card can still pick its own.">
                            <field name="dbc_default_template"/>
                        </setting>
                    </block>
                    <block title="Publishing" name="dbc_publishing_block">
                        <setting string="Publishing Address"
                                 help="Base web address used in every published card's link and QR code (e.g. https://cards.example.com). Leave empty to use Odoo's own base URL.">
                            <field name="dbc_public_base_url" placeholder="https://cards.example.com"/>
                        </setting>
                        <setting string="Publish Targets"
                                 help="External web hosts that generated cards are sent to (POST/PUT).">
                            <div class="text-muted">
                                <field name="dbc_target_count" class="oe_inline"/> target(s) configured
                            </div>
                            <div class="mt8">
                                <button name="action_open_dbc_targets" type="object"
                                        string="Manage Publish Targets"
                                        icon="fa-arrow-right" class="btn-link"/>
                            </div>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <!-- Open Settings scoped to this app from the Business Cards menu. -->
    <record id="action_dbc_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module': 'digital_business_card'}</field>
    </record>

</odoo>
